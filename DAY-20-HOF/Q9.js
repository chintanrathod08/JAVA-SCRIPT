// Write a function that takes an array of strings and returns a new array where each string is converted to uppercase. Use the map method.

let arr = ['hello', 'world', 'javascript']

let world = arr.map((ele)=>{

    return ele.toUpperCase()    

})

console.log(world);



